rules_version = '2';

// Alles dicht tot de echte regels er zijn (fase 1).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
